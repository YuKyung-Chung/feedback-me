Verify the generated Korean feedback report against the structured analyses and evidence references below.
Return only valid JSON with:
- valid: boolean
- issues: array of strings
- correctedReport: string (use the original report unchanged when valid; otherwise provide a corrected report)
Do not invent facts or evidence. Remove unsupported claims and preserve only claims supported by the analyses.

REPORT:
{{report}}

STRUCTURED ANALYSES:
{{analyses}}
